{
  "name": "yg-abbruch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
